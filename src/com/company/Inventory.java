package com.company;
import java.util.Scanner;

public class Inventory extends Runner{
    Scanner request1 = new Scanner(System.in);
    String inventory = request1.nextLine();

    if (inventory.equals())
}
